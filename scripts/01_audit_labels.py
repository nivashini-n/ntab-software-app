"""Step 1 — full annotation audit across the dev pool (holdout stays locked).

Verifies T1/T2 semantics, trial counts/durations, class balance, sampling rates;
re-confirms the documented defects of the 6 excluded subjects; writes
results/audit.csv + S1 gallery figures.
Usage: uv run python scripts/01_audit_labels.py
TODO(Phase A): implement via mi_decoding.labels + viz.fig_s1_label_audit.
"""

raise SystemExit("TODO(Phase A): not implemented yet — see DECISIONS.md.")

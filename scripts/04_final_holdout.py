"""Step 4 — THE single-shot locked-holdout evaluation (rung 3). Run ONCE, Phase C.

The only place in the codebase where unlock_holdout=True is passed. Evaluates the
frozen cross-subject model on the 20 locked subjects; reports accuracy + binomial CI.
Usage: uv run python scripts/04_final_holdout.py
TODO(Phase C).
"""

raise SystemExit("TODO(Phase C): locked until the final evaluation — see the DECISIONS.md pre-commitments.")

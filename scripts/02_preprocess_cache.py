"""Step 2 — filter → epoch → reject → cache (X, y, subject, run) for dev pool.

Generates S0 (raw), S2 (filtering), S3 (epoching), S4 (rejection) gallery figures
as the data flows through each stage.
Usage: uv run python scripts/02_preprocess_cache.py [--task imagery|executed]
TODO(Phase A): implement via mi_decoding.preprocess + viz stages S0–S4.
"""

raise SystemExit("TODO(Phase A): not implemented yet — see DECISIONS.md.")
